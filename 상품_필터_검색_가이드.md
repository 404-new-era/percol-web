# 상품 목록 — 카테고리/검색/정렬 필터 (프론트 연동)

> 백엔드에 **이미 다 구현돼 있음.** 프론트는 쿼리스트링만 붙여 호출하면 됨.
> 운영 API: `https://43.201.8.235.sslip.io/api/v1`

## 엔드포인트
```
GET /products?{필터들}
```

## 쿼리 파라미터 (전부 선택, 조합 가능)
| 파라미터 | 값 | 설명 |
|---|---|---|
| `category` | `상의` `하의` `아우터` `원피스` `잡화` | **카테고리 필터** (현재 DB에 있는 값) |
| `source` | `TOPTEN` `UNIQLO` `29CM` | **브랜드/출처 필터** (탑텐/유니클로/29CM) |
| `season` | `SPRING` `SUMMER` `FALL` `WINTER` | 퍼스널컬러 시즌 필터 |
| `tone` | `WARM` `COOL` | 톤 필터 |
| `keyword` | 문자열 | 상품명·브랜드 검색 (부분일치, 대소문자 무시) |
| `minPrice` / `maxPrice` | 숫자 | 가격 범위 |
| `sort` | `mixed`(기본) `price_asc` `price_desc` | 정렬 (기본=29CM 우선+섞임) |
| `page` / `limit` | 숫자 | 페이지네이션 (limit 기본 20, 최대 50) |

> 카테고리 값은 한글이라 URL 인코딩 필요 (axios/fetch가 자동 처리).

## 예시
```ts
// 카테고리별
api.get('/products', { params: { category: '아우터' } })
// 카테고리 + 시즌(진단결과) + 정렬
api.get('/products', { params: { category: '하의', season: 'WINTER', sort: 'price_asc' } })
// 검색 + 가격범위 + 페이지
api.get('/products', { params: { keyword: '윈드브레이커', maxPrice: 50000, page: 1, limit: 20 } })
```

## 응답
```jsonc
{
  "success": true,
  "data": [
    {
      "id": "ck...", "name": "UV프로텍션 라이트 윈드브레이커",
      "source": "TOPTEN", "brand": "탑텐",   // ← 브랜드/출처 구분용 (뱃지 표시)
      "category": "아우터", "price": 49900,
      "originalPrice": 70000, "discountRate": 29,  // 세일 시에만(아니면 null) — 정가/할인율
      "imageUrl": "https://img.goodwearmall.com/goods/.../..._M.jpg",  // 실제 상품 이미지
      "productUrl": "https://topten10.goodwearmall.com/product/.../detail",
      "colorName": "라이트핑크", "colorHex": "#...",
      "seasonTags": [ { "season": "SUMMER", "tone": "COOL" } ]
    }
  ],
  "meta": { "page": 1, "limit": 20, "total": 42 }
}
```

## 현재 데이터 분포 (참고)
- 전체 522개 (실제 탑텐 상품, 진짜 이미지)
- 상의 229 · 하의 161 · 아우터 42 · 원피스 30 · 잡화 60

## UI 제안
- **카테고리 탭/칩**: `상의 하의 아우터 원피스 잡화` → 클릭 시 `category=` 붙여 재호출
- **정렬 드롭다운**: 최신순 / 낮은가격 / 높은가격 → `sort=recent|price_asc|price_desc`
- **검색창**: 입력값 → `keyword=`
- **무한스크롤/페이지**: `page` 증가, `meta.total`로 끝 판단
- (진단 연동) 마이 시즌으로 자동 필터: `season=내시즌` 기본 적용 + 사용자가 끌 수 있게

> 카테고리 목록을 하드코딩하기 싫으면, 그냥 위 5개를 칩으로 두면 됨 (당분간 고정).
> 전체 스펙은 같은 폴더 `openapi.json` 참고.
